services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: whirlyball
      POSTGRES_USER: whirlyball
      POSTGRES_PASSWORD: whirlyball
    ports:
      - "55432:5432"
    volumes:
      - whirlyball-postgres:/var/lib/postgresql/data

volumes:
  whirlyball-postgres:
