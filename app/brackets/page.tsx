export const dynamic = "force-dynamic";

export default function BracketsPage() {
  return (
    <main className="content bracket-page">
      <section className="section card">
        <p className="eyebrow">Tournament Bracket</p>
        <h1>Bracket Handled Manually</h1>
        <p className="muted">
          The elimination bracket will be managed outside the app. Use the public schedule for seeding games, Unlimited timing blocks, court streams, and replay links.
        </p>
      </section>
    </main>
  );
}
